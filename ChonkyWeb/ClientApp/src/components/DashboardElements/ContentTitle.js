import React from 'react'

const ContentTitle = ({title}) => (
    <h3 style={{minHeight: '63px', display:'flex', alignItems: 'center', fontWeight: 'bold'}}>{title}</h3>
)

export default ContentTitle